const puppeteer = require('puppeteer'); (async () => { try { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.setViewport({ width: 390, height: 844 }); await page.goto('http://localhost:3000/login'); await new Promise(r => setTimeout(r, 1000)); const ml = await page.evaluate(() => { const el = document.querySelector('.main-wrapper'); return el ? window.getComputedStyle(el).marginLeft : 'NOT_FOUND'; }); console.log('main-wrapper margin-left:', ml); const display = await page.evaluate(() => { const el = document.querySelector('.sidebar'); return el ? window.getComputedStyle(el).display : 'NOT_FOUND'; }); console.log('sidebar display:', display); await browser.close(); } catch(e) { console.error(e); } })();
